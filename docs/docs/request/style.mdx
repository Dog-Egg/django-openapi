import CodeBlock from '@theme/CodeBlock';
import CodeSource from '!!raw-loader!@site/snippets/resources/parameter_styles.py'

# 参数风格

<UnderConstruction/>

OpenAPI 3.0 定义了多种[参数风格](https://spec.openapis.org/oas/v3.0.3#style-values)，我们可以在 Schema 上定义 `Style` 以实现不同风格的参数解析。

请参考以下示例，选择自己需要的风格。

<CodeBlock language="python">{CodeSource}</CodeBlock>

<SwaggerUI spec="parameter_styles"/>