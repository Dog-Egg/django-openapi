---
sidebar_position: 200
---

import CodeBlock from '@theme/CodeBlock';
import CodeSource from '!!raw-loader!@site/examples/views/query_parameter.py'
import CodeSource2 from '!!raw-loader!@site/examples/views/query_parameter_blank.py'
import CodeSource3 from '!!raw-loader!@site/examples/views/query_parameter_default.py'

# Query 参数

<UnderConstruction/>

<CodeBlock language="python">{CodeSource}</CodeBlock>

<SwaggerUI spec="query_parameter" height={300}/>

## 默认值

设置 `default` 参数
:::tip
设置 `default` 后，`required` 将会被设置为 False
:::

<CodeBlock language="python">{CodeSource3}</CodeBlock>

<SwaggerUI spec="query_parameter_default" height={300}/>

## 空值处理

客户端可能会传递这样的参数：`/path/xxx?a=&b=`，默认情况下，参数解析时会忽略这些空值，如果需要获取空值，设置 `allow_blank = True`

<CodeBlock language="python">{CodeSource2}</CodeBlock>

<SwaggerUI spec="query_parameter_blank" height={300}/>
