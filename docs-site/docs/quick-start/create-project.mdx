# 从零开始

## 创建一个 Django 项目

```bash
pip install django # 安装 Django

django-admin startproject myproject # 初始化项目

cd myproject
python manage.py startapp books # 初始化一个 books 应用
```

我们将得到以下的目录结构

```
myproject
├── books
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── migrations
│   │   └── __init__.py
│   ├── models.py
│   ├── tests.py
│   └── views.py
├── manage.py
└── myproject
    ├── __init__.py
    ├── asgi.py
    ├── settings.py
    ├── urls.py
    └── wsgi.py
```

_更多项目创建说明查看 [Django 文档](https://docs.djangoproject.com/zh-hans/4.0/intro/tutorial01/)_

## 安装 Django-OpenAPI

```bash
pip install git+https://github.com/Dog-Egg/django-openapi.git
```

在项目 setting 文件中的 `INSTALLED_APPS` 添加 books 应用 和 Django-OpenAPI 应用

```jsx title="/myproject/settings.py"
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    // highlight-start
    'books',
    'openapi'
    // highlight-end
]
```

## 创建一个简单 API

```python title="/books/views.py"
from django.http import JsonResponse
from openapi.core import API

class BooksAPI(API):
    def get(self, request):
        return JsonResponse({})
```

然后将 `BooksAPI` 添加到 `OpenAPI` 中

```python title="/myproject/urls"
from django.contrib import admin
from django.urls import path
# highlight-start
from openapi.core import OpenAPI
from openapi.views import swagger_ui

from books import views

openapi = OpenAPI(title='This is example')
openapi.add_route('books', views.BooksAPI)
# highlight-end

urlpatterns = [
    # highlight-start
    path('', openapi.urls),
    path('api-spec', openapi.api_spec, name='api-spec'),
    path('swagger-ui', swagger_ui('api-spec')),
    # highlight-end

    path('admin/', admin.site.urls),
]
```

现在运行项目，并在浏览器访问 http://127.0.0.1:8000/swagger-ui

```bash
python manage.py runserver
```

<BrowserWindow url="http://127.0.0.1:8000/swagger-ui">123</BrowserWindow>